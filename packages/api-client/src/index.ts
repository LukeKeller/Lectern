import type { Card } from "@lectern/shared";

/**
 * Placeholder for the typed Lectern API client.
 *
 * In D2 this is generated from the BFF's OpenAPI contract. For now it only
 * re-exports the shared types so downstream packages can depend on it.
 */
export type { Card };
